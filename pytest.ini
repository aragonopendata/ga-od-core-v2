[pytest]
markers=
  this: mark a test to be run in this session

timeout = 10

log_cli=true
log_level=WARNING
log_format = %(asctime)s %(levelname)s %(message)s
log_date_format = %Y-%m-%d %H:%M:%S
addopts = -p no:warnings
tbstyle = short
